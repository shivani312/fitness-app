import { Route, Routes } from "react-router-dom";

import "./assets/styles/app.scss";

const App = () => {
    return (
        <Routes>
            <Route path="/" element />
        </Routes>
    );
};

export default App;
